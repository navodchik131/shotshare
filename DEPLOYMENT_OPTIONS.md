# Варианты деплоя ShotShare Bot

## Нужен ли домен?

**Короткий ответ: НЕТ, домен не обязателен для базовой работы!**

Но есть нюансы в зависимости от того, как вы хотите развернуть проект.

---

## Варианты деплоя

### Вариант 1: Polling (без домена) ✅ РЕКОМЕНДУЕТСЯ для начала

**Как работает:**
- Бот постоянно опрашивает Telegram API на наличие новых сообщений
- Не требует домена или HTTPS
- Работает через обычное подключение к интернету

**Что нужно:**
- Сервер с IP-адресом (VPS, облачный сервер)
- Или даже ваш ПК с постоянным интернетом (не рекомендуется для продакшена)

**Плюсы:**
- ✅ Не нужен домен
- ✅ Не нужен SSL сертификат
- ✅ Простая настройка
- ✅ Работает из любой сети

**Минусы:**
- ⚠️ Постоянное подключение к интернету
- ⚠️ Больше нагрузка на сервер (постоянные запросы)

**Текущая реализация:** Используется polling (`application.run_polling()`)

---

### Вариант 2: Webhook (требует домен) 

**Как работает:**
- Telegram отправляет обновления на ваш сервер по HTTPS
- Требует домен и SSL сертификат

**Что нужно:**
- Домен (например, `yourbot.com`)
- SSL сертификат (можно бесплатный от Let's Encrypt)
- Настроенный webhook

**Плюсы:**
- ✅ Меньше нагрузка на сервер
- ✅ Более эффективно для больших ботов
- ✅ Стандартный подход для продакшена

**Минусы:**
- ❌ Нужен домен
- ❌ Нужен SSL сертификат
- ❌ Сложнее настройка

---

## Админка (Flask приложение)

### Без домена (IP-адрес)

**Можно запустить на:**
- IP-адрес сервера: `http://123.45.67.89:5000`
- Локальная сеть: `http://192.168.1.100:5000`

**Плюсы:**
- ✅ Не нужен домен
- ✅ Быстрый старт

**Минусы:**
- ⚠️ Небезопасно (HTTP вместо HTTPS)
- ⚠️ Нужно помнить IP-адрес
- ⚠️ Может меняться при перезапуске сервера

### С доменом (рекомендуется)

**Можно использовать:**
- Собственный домен: `https://admin.yourbot.com`
- Бесплатные домены: Freenom, No-IP (с ограничениями)
- Поддомены: `https://shotshare.yourdomain.com`

**Плюсы:**
- ✅ Безопасно (HTTPS)
- ✅ Удобно запомнить
- ✅ Стабильный адрес

**Минусы:**
- ❌ Нужен домен (от $1-10/год)
- ❌ Нужен SSL сертификат (можно бесплатный)

---

## Рекомендации по деплою

### Для начала (MVP/тестирование):

1. **VPS сервер** (DigitalOcean, Hetzner, Timeweb и т.д.)
   - Цена: от $5-10/месяц
   - IP-адрес: есть
   - Домен: не нужен

2. **Настройка:**
   - Бот: использует polling (как сейчас)
   - Админка: доступна по IP `http://YOUR_IP:5000`
   - База данных: PostgreSQL на том же сервере

3. **Безопасность:**
   - Используйте firewall (только нужные порты)
   - Сильный пароль для админки
   - Не открывайте админку в интернет без защиты

### Для продакшена:

1. **Домен** (желательно, но не обязательно)
   - Можно купить за $1-10/год
   - Или использовать бесплатные варианты

2. **SSL сертификат** (если есть домен)
   - Let's Encrypt - бесплатный
   - Автоматическое обновление через certbot

3. **Nginx/Apache** (реверс-прокси)
   - Для админки через HTTPS
   - Можно без домена, но лучше с

---

## Где можно развернуть без домена:

### 1. VPS серверы (рекомендуется)
- **DigitalOcean**: от $6/месяц
- **Hetzner**: от €4/месяц
- **Timeweb**: от 200₽/месяц
- **Selectel**: от 300₽/месяц

### 2. Облачные платформы
- **Yandex Cloud**: есть бесплатный период
- **AWS EC2**: есть бесплатный tier
- **Google Cloud**: есть бесплатный tier

### 3. Docker-хостинг
- **Railway**: бесплатный tier
- **Render**: бесплатный tier
- **Fly.io**: бесплатный tier

---

## Минимальная конфигурация для деплоя:

```yaml
# docker-compose.yml (уже есть)
services:
  db:      # PostgreSQL
  bot:     # Telegram бот (polling, домен не нужен)
  admin:   # Flask админка (можно по IP)
```

**Что нужно изменить:**
1. Обновить `DATABASE_URL` в `.env` для продакшена
2. Убедиться, что порт 5000 открыт (или использовать другой)
3. Настроить firewall
4. (Опционально) Добавить домен и SSL для админки

---

## Итог:

**Для начала:**
- ✅ Домен НЕ нужен
- ✅ Можно использовать IP-адрес сервера
- ✅ Бот работает через polling (как сейчас)
- ✅ Админка доступна по `http://IP:5000`

**Для продакшена:**
- 💡 Домен желателен (но не обязателен)
- 💡 SSL сертификат для админки (если есть домен)
- 💡 Можно оставить polling или перейти на webhook

**Вывод:** Можете развернуть проект на любом VPS с IP-адресом без домена. Домен нужен только для удобства и безопасности админки, но не критичен для работы бота.

